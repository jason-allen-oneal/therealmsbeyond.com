import type { NextPage } from "next";
import Layout from "../components/Layout";

const About: NextPage = () => {
  const data = {
    title: "About Us",
    description: "About TheRealmsBeyond",
  };

  return (
    <></>
  );
};

export default About;
