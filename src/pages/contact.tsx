import type { NextPage } from "next";
import Layout from "../components/Layout";

const Contact: NextPage = () => {
  const data = {
    title: "Contact Us",
    description: "Contact TheRealmsBeyond",
  };

  return (
    <></>
  );
};

export default Contact;
